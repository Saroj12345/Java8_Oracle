package array;

/* Smallest number which can be divisible by both the numbers*/
public class LCM {

    public static void main(String[] args) {

        int a = 10;
        int b = 15;

        /*int lcm = a > b ? a : b;

        while (true) {

            if (lcm % a == 0 && lcm % b == 0) {
                System.out.println("LCM Found " + lcm);
                break;
            }
            lcm++;
        }*/
        //calculate GCD and then (a*b)/GCD
    }
}
